const backGround = document.querySelector("#main_img");

const buttons = document.querySelectorAll(".carousel_button");

for (let button of buttons) {
    // button.addEventListener("click", () => {
    //     if (button.classList.includes(".main_img"))
    // })
    console.log(Array.isArray(backGround.classList))
}
